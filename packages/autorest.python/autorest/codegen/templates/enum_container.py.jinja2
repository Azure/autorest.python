# coding=utf-8
{{ code_model.options['license_header'] }}

from enum import Enum
from {{ code_model.core_library }}{{ ".utils" if not code_model.is_azure_flavor else "" }} import CaseInsensitiveEnumMeta

{% for enum in code_model.enums | sort %}
{% include "enum.py.jinja2" %}
{% endfor %}
