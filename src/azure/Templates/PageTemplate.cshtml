@using System
@using System.Linq
@using System.Collections.Generic
@using AutoRest.Python
@inherits AutoRest.Core.Template<System.Collections.Generic.List<AutoRest.Python.Azure.Model.PagePya>>
# coding=utf-8
# --------------------------------------------------------------------------
@Header("# ").TrimMultilineHeader()
# --------------------------------------------------------------------------
@EmptyLine
from msrest.paging import Paged
@EmptyLine
@EmptyLine
@foreach(var pagedModel in Model) {
<text>
class @(pagedModel.TypeDefinitionName)(Paged):
    """
    A paging container for iterating over a list of @(pagedModel.GetReturnTypeDocumentation()) object
    """
@EmptyLine
    _attribute_map = {
        'next_link': {'key': '@pagedModel.NextLinkName', 'type': 'str'},
        'current_page': {'key': '@pagedModel.ItemName', 'type': '[@pagedModel.ItemType.Name]'}
    }
@EmptyLine
    def __init__(self, *args, **kwargs):
@EmptyLine
        super(@(pagedModel.TypeDefinitionName), self).__init__(*args, **kwargs)
</text>
}